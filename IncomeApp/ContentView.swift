import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello world!")
                .font(.system(size: 16, weight: .regular))
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
